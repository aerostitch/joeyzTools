# HTTP-related scripts

## `replay`

Reads files containing base64-encoded HTTP request and replay them against a
given target.

## `benchmark`

The script in `benchmark` calls the given URL a given amount of time and returns the statistics about the http calls in a CSV format.
